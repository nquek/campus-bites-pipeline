services:
  db:
    image: postgres:16
    container_name: campus_bites_db
    environment:
      POSTGRES_DB: campus_bites
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - ./data/campus_bites_orders.csv:/data/campus_bites_orders.csv
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
